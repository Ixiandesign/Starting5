# Starting5

This file is an index. It stays short — details live in the docs it points to.

## Agent skills

### Issue tracker

Issues and specs live as GitHub issues on `Ixiandesign/Starting5`, via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout — `CONTEXT.md` + `docs/adr/` at the repo root (created lazily by `/domain-modeling` as terms/decisions are resolved). See `docs/agents/domain.md`.
